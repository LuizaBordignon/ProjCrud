<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Loja de Eletrônicos</h1>
    <ul>
        <% produtos.forEach(function(produto, index) { %>
            <li>
                <%= produto.nome %> - R$ <%= produto.preco %>
                
                <a href="/deletar/<%= index %>" style="color: red;">[Excluir]</a>
            </li>
        <% }); %>
    </ul>
</body>
</html>